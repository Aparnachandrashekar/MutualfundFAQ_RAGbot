# Phase 2 — Chunking, indexing, and retrieval

Reserved for Phase 2 implementation per [PhaseWiseArchitecture.md](../../PhaseWiseArchitecture.md).

**Planned contents:** chunking pipeline, embeddings, vector store, retrieval API with metadata (`source_url`, `scheme_id`, `ingested_at`).

**Exit criteria:** deterministic citation URL mapping from retrieved chunks; golden-set QA.
